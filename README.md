# Ulixarm
Repo containing code to be used with ROS2 control or with Python bindings to interact directly with the motors using Python.

In the cloning, use --recursive option to also retrieve the submodules.

To compile the Python bindings, follow instruction in src/damiao_hardware_interface.

